# === Required ===
RS_GROUP_API_KEY=your-engine-api-key-here
STORE_NAME=THE GUN FIRM

# Engine endpoint (keep default unless you have a different engine domain)
ENGINE_ORDER_URL=https://engine.thegunfirmapp.com/webhook/api/rsr/create-order

# === Optional ===
# Fallback FFL license if no FFL is attached to an order (use a valid FFL in production)
FFL_FALLBACK=1-23-45678

# Port for the Express app
PORT=3000

# If you later add direct RSR calls, you might use these (not needed for engine flow):
# RSR_USERNAME=
# RSR_PASSWORD=
# RSR_POS=I